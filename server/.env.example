# Copiá este archivo a .env y completá los valores.

# API key de Anthropic para el juez IA (Luján, roast, padrino).
# Sin key, el juez usa un fallback determinístico (el juego funciona igual).
ANTHROPIC_API_KEY=

# Puerto del server (Railway lo setea solo).
PORT=3001
